# Tiny Vector Search Demo

This is a beginner-friendly AI project exploring vector similarity search.

## Requirements
- Python 3.x
- sentence-transformers (`pip install sentence-transformers`)

## How to run
1. Clone the repo
2. Run `python main.py`

## What I learned
- Using pre-trained models to generate embeddings
- Comparing similarity between text using cosine similarity
